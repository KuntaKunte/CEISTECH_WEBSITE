version: '3.8'

services:
  ceistech:
    build: .
    # Map host port to container port using the PORT environment variable (defaults to 3002)
    ports:
      - "${PORT:-3002}:${PORT:-3002}"
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-3002}
    restart: unless-stopped
